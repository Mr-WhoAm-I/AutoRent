<Page x:Class="CarRental.UI.Views.Pages.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      Title="Сводная аналитика" FontFamily="Segoe UI" Loaded="Page_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="400"/>  </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,25">
                <TextBlock Text="Сводная аналитика" FontSize="26" FontWeight="Bold" Foreground="#333"/>
                <TextBlock x:Name="TxtDate" Text="Данные за Текущий Месяц" FontSize="14" Foreground="Gray" Margin="0,5,0,0"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,30">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="15" Height="120">
                    <Border.Effect><DropShadowEffect Color="#E5E9F2" BlurRadius="15" Opacity="0.5"/></Border.Effect>
                    <Grid Margin="20">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="ДОХОДЫ (МЕС)" FontSize="11" FontWeight="Bold" Foreground="#90A4AE"/>
                            <TextBlock x:Name="TxtIncome" Text="0.00 BYN" FontSize="26" FontWeight="Bold" Foreground="#00C853" Margin="0,5"/>
                            <TextBlock Text="Сумма платежей" FontSize="11" Foreground="#90A4AE"/>
                        </StackPanel>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="10" Background="#E8F5E9">
                            <TextBlock Text="💰" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="15" Height="120">
                    <Border.Effect><DropShadowEffect Color="#E5E9F2" BlurRadius="15" Opacity="0.5"/></Border.Effect>
                    <Grid Margin="20">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="РАСХОДЫ (МЕС)" FontSize="11" FontWeight="Bold" Foreground="#90A4AE"/>
                            <TextBlock x:Name="TxtExpense" Text="0.00 BYN" FontSize="26" FontWeight="Bold" Foreground="#F44336" Margin="0,5"/>
                            <TextBlock Text="ТО + Страховки" FontSize="11" Foreground="#90A4AE"/>
                        </StackPanel>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="10" Background="#FFEBEE">
                            <TextBlock Text="📉" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>

                <Border Grid.Column="4" Background="White" CornerRadius="15" Height="120">
                    <Border.Effect><DropShadowEffect Color="#E5E9F2" BlurRadius="15" Opacity="0.5"/></Border.Effect>
                    <Grid Margin="20">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="АКТИВНЫЕ АРЕНДЫ" FontSize="11" FontWeight="Bold" Foreground="#90A4AE"/>
                            <TextBlock x:Name="TxtActiveRentals" Text="0" FontSize="26" FontWeight="Bold" Foreground="#333" Margin="0,5"/>
                            <TextBlock Text="Автомобилей у клиентов" FontSize="11" Foreground="#90A4AE"/>
                        </StackPanel>
                        <Border HorizontalAlignment="Right" VerticalAlignment="Top" Width="40" Height="40" CornerRadius="10" Background="#E3F2FD">
                            <TextBlock Text="🔑" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="15">
                    <Border.Effect><DropShadowEffect Color="#E5E9F2" BlurRadius="15" Opacity="0.5"/></Border.Effect>
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Динамика финансов (по дням)" FontSize="16" FontWeight="Bold" Foreground="#333"/>

                        <lvc:CartesianChart Grid.Row="1" Margin="0,15,0,0" LegendLocation="Bottom">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Дни месяца" Labels="{Binding ChartLabels}"/>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Сумма (BYN)" MinValue="0"/>
                            </lvc:CartesianChart.AxisY>
                            
                            <lvc:CartesianChart.Series>
                                <lvc:LineSeries Title="Доходы" Values="{Binding IncomeValues}" Stroke="#00C853" Fill="#2000C853" PointGeometrySize="10"/>
                                <lvc:LineSeries Title="Расходы" Values="{Binding ExpenseValues}" Stroke="#F44336" Fill="#20F44336" PointGeometrySize="10"/>
                            </lvc:CartesianChart.Series>
                        </lvc:CartesianChart>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Background="White" CornerRadius="15">
                    <Border.Effect><DropShadowEffect Color="#E5E9F2" BlurRadius="15" Opacity="0.5"/></Border.Effect>
                    <Grid Margin="20">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Состояние парка" FontSize="16" FontWeight="Bold" Foreground="#333"/>

                        <lvc:PieChart x:Name="StatusPieChart" Grid.Row="1" 
                                      LegendLocation="Bottom" Hoverable="True" 
                                      InnerRadius="40" Margin="0,10,0,0">
                        </lvc:PieChart>
                        
                        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center" IsHitTestVisible="False">
                            <TextBlock x:Name="TxtTotalCars" Text="0" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <TextBlock Text="Всего авто" FontSize="10" Foreground="Gray" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</Page>