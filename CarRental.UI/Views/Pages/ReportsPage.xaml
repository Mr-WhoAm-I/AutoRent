<Page x:Class="CarRental.UI.Views.Pages.ReportsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Генератор отчетов" FontFamily="Segoe UI">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0,0,10,0">
        <StackPanel>
            <TextBlock Text="Генератор отчетов" FontSize="26" FontWeight="Bold" Foreground="#333" Margin="0,0,0,25"/>

            <Border Background="White" CornerRadius="10" Margin="0,0,0,30">
                <Border.Effect>
                    <DropShadowEffect Color="#E5E9F2" BlurRadius="15" ShadowDepth="3" Opacity="0.5"/>
                </Border.Effect>
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <Path Data="M14 2H6c-1.1 0-1.99.9-1.99 2L4 20c0 1.1.89 2 1.99 2H18c1.1 0 2-.9 2-2V8l-6-6zm2 16H8v-2h8v2zm0-4H8v-2h8v2zm-3-5V3.5L18.5 9H13z" 
                              Fill="#5C6BC0" Width="16" Height="16" Stretch="Uniform"/>
                        <TextBlock Text="Параметры отчета" FontWeight="SemiBold" FontSize="16" Margin="10,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="15"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0">
                            <TextBlock Text="Тип отчета" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                            <ComboBox Height="40" SelectedIndex="0" Padding="10,0">
                                <ComboBoxItem Content="Финансовый отчет (Выручка)"/>
                                <ComboBoxItem Content="Популярность автомобилей"/>
                                <ComboBoxItem Content="Задолженности клиентов"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Row="0" Grid.Column="2">
                            <TextBlock Text="Класс / Категория" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                            <TextBox Height="40" Padding="10,0" VerticalContentAlignment="Center" BorderBrush="#E0E0E0"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="0">
                            <TextBlock Text="Начало периода" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                            <DatePicker Height="40" Padding="5,0" VerticalContentAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Row="2" Grid.Column="2">
                            <TextBlock Text="Конец периода" Foreground="Gray" FontSize="12" Margin="0,0,0,5"/>
                            <DatePicker Height="40" Padding="5,0" VerticalContentAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <Button Grid.Row="2" Content="Сформировать отчет" 
                            HorizontalAlignment="Right" Width="200" Height="45" Margin="0,30,0,0"
                            Foreground="White" FontSize="14" FontWeight="SemiBold" Cursor="Hand" BorderThickness="0">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#5C6BC0" Offset="0"/>
                                <GradientStop Color="#3949AB" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>
            </Border>

            <Border Background="White" CornerRadius="10" Margin="0,0,0,30">
                <Border.Effect>
                    <DropShadowEffect Color="#E5E9F2" BlurRadius="15" ShadowDepth="3" Opacity="0.5"/>
                </Border.Effect>
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <TextBlock Text="Предпросмотр" FontWeight="SemiBold" FontSize="16" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="🖨️" Width="35" Height="35" Margin="0,0,10,0" Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1"/>
                            <Button Content="📥" Width="35" Height="35" Background="Transparent" BorderBrush="#E0E0E0" BorderThickness="1"/>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="1" BorderBrush="#EEEEEE" BorderThickness="1" Background="#FAFAFA" Padding="40">
                        <StackPanel>
                            <TextBlock Text="AUTORENT PRO" FontSize="20" FontWeight="Bold" FontFamily="Times New Roman" HorizontalAlignment="Center" Foreground="#1A237E"/>
                            <TextBlock Text="Отчет о финансовых показателях" FontSize="14" HorizontalAlignment="Center" Margin="0,5,0,0" Foreground="Gray"/>
                            <TextBlock Text="Период: 01.01.2023 — 31.12.2023" FontSize="12" HorizontalAlignment="Center" Margin="0,2,0,30" Foreground="Gray"/>

                            <DataGrid AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" 
                                      Background="Transparent" BorderThickness="0" GridLinesVisibility="Horizontal"
                                      HorizontalGridLinesBrush="#EEE" RowBackground="Transparent">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="КАТЕГОРИЯ" Width="*" Binding="{Binding Category}" FontWeight="Bold"/>
                                    <DataGridTextColumn Header="КОЛ-ВО АРЕНД" Width="*" Binding="{Binding Count}"/>
                                    <DataGridTextColumn Header="СРЕДНИЙ ЧЕК" Width="*" Binding="{Binding AvgCheck}"/>
                                    <DataGridTextColumn Header="ВЫРУЧКА" Width="*" Binding="{Binding Revenue}" FontWeight="Bold"/>
                                </DataGrid.Columns>
                            </DataGrid>

                            <Grid Margin="0,20,0,0">
                                <TextBlock Text="ИТОГО" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="1 245 000 ₽" FontWeight="Bold" FontSize="16" Foreground="#1A237E" HorizontalAlignment="Right"/>
                            </Grid>

                            <TextBlock Text="Сгенерировано системой AutoRent Pro" FontSize="10" Foreground="#CCC" Margin="0,40,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>