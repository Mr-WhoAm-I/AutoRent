<Window x:Class="CarRental.UI.Views.Windows.BookingAddWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="Бронирование" Height="520" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown" FontFamily="Segoe UI">

    <Border Background="White" CornerRadius="15" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock x:Name="TxtTitle" Text="Новое бронирование" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Style="{StaticResource WindowControlButtonStyle}" Foreground="#999" Click="Close_Click"/>
            </Grid>

            <StackPanel Grid.Row="1">

                <TextBlock Text="1. Период" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <hc:DatePicker x:Name="DateStart" hc:InfoElement.Placeholder="Начало" 
                                   SelectedDateChanged="Date_Changed" Height="45"/>
                    <TextBlock Text="—" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Gray"/>
                    <hc:DatePicker x:Name="DateEnd" Grid.Column="2" hc:InfoElement.Placeholder="Окончание" 
                                   SelectedDateChanged="Date_Changed" Height="45"/>
                </Grid>

                <TextBlock Text="2. Автомобиль" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                <ComboBox x:Name="ComboCar" Height="50" Margin="0,0,0,15"
                          Style="{StaticResource FloatingHintComboBox}" Tag="Выберите автомобиль"
                          IsEnabled="False"
                          DisplayMemberPath="DisplayName"/>

                <TextBlock Text="3. Клиент" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                <ComboBox x:Name="ComboClient" Height="50" Margin="0,0,0,15"
                          Style="{StaticResource FloatingHintComboBox}" Tag="Выберите клиента"
                          DisplayMemberPath="FullName"/>

                <TextBlock Text="4. Комментарий" FontWeight="Bold" Foreground="#555" Margin="0,0,0,5"/>
                <TextBox x:Name="TxtComment" Height="60" TextWrapping="Wrap" VerticalContentAlignment="Top" Padding="10"
                         BorderBrush="#C0C0C0" BorderThickness="1" Background="White"/>

            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Отмена" Style="{StaticResource ButtonDashed}" Width="100" Height="40" Margin="0,0,10,0" Click="Close_Click"/>
                <Button x:Name="BtnSave" Content="Забронировать" Style="{StaticResource ButtonPrimary}" Background="{StaticResource GreenGradientBrush}" 
                        BorderThickness="0" Width="140" Height="40" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>