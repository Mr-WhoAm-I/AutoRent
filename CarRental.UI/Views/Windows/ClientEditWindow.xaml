<Window x:Class="CarRental.UI.Views.Windows.ClientEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        Title="Клиент" Height="550" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown" FontFamily="Segoe UI">

    <Border Background="White" CornerRadius="15" BorderBrush="#E0E0E0" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
        </Border.Effect>

        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock x:Name="TxtTitle" Text="Новый клиент" FontSize="22" FontWeight="Bold" Foreground="#333"/>
                <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top"
                        Style="{StaticResource WindowControlButtonStyle}" Foreground="#999" Click="Close_Click"/>
            </Grid>

            <StackPanel Grid.Row="1">
                <TextBox x:Name="TxtSurname" Style="{StaticResource FloatingHintTextBox}" Tag="Фамилия"/>
                <TextBox x:Name="TxtName" Style="{StaticResource FloatingHintTextBox}" Tag="Имя" Margin="0,10,0,0"/>
                <TextBox x:Name="TxtPatronymic" Style="{StaticResource FloatingHintTextBox}" Tag="Отчество" Margin="0,10,0,0"/>

                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="TxtPhone" Style="{StaticResource FloatingHintTextBox}" Tag="Телефон"/>
                    <TextBox x:Name="TxtEmail" Grid.Column="2" Style="{StaticResource FloatingHintTextBox}" Tag="Email"/>
                </Grid>

                <Grid Margin="0,15,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="Дата рождения" Foreground="Gray" FontSize="12" Margin="2,0,0,5"/>
                        <hc:DatePicker x:Name="DateBirth" Height="45" BorderThickness="1" BorderBrush="#C0C0C0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Стаж вождения (лет)" Foreground="Gray" FontSize="12" Margin="2,0,0,5"/>
                        <TextBox x:Name="TxtExp" Height="45" VerticalContentAlignment="Center" Padding="10,0"
                                 BorderBrush="#C0C0C0" BorderThickness="1" Background="White"
                                 PreviewTextInput="NumberValidation"/>
                    </StackPanel>
                </Grid>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Отмена" Style="{StaticResource ButtonDashed}" Width="100" Height="40" Margin="0,0,10,0" Click="Close_Click"/>
                <Button Content="Сохранить" Style="{StaticResource ButtonPrimary}" Background="{StaticResource GreenGradientBrush}" 
                        BorderThickness="0" Width="120" Height="40" Click="Save_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>